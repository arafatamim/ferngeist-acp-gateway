# Generated repo artifacts must never get line-ending conversion — git's
# autocrlf would corrupt binary formats and break GPG signatures over the
# exact bytes (a signed LF file checks out as CRLF and fails verification).
# All files under deb/ and rpm/ are machine-generated by the release
# workflow; treat them as binary (no EOL normalization).
deb/** -text
rpm/** -text
